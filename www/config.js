/* =====================================================================
   Zugang zur Datenbank.
   Beide Werte findest du in Supabase unter Project Settings → API.
   Bleiben sie leer, läuft die App wie bisher rein auf dem Gerät.

   Der anon key darf öffentlich sein. Er erlaubt für sich genommen
   keinen Zugriff auf Daten — das regeln die Sicherheitsregeln in der
   Datenbank. Den service_role key trägst du hier NIEMALS ein.
   ===================================================================== */
window.BUCHFIX_CONFIG = {
  SUPABASE_URL: "",
  SUPABASE_ANON_KEY: ""
};
